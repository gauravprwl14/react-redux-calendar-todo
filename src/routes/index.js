// import CheckTyping from './CheckTyping'
import Calendar from './Calendar'
const appRoutes = [
  {
    path: "/",
    component: Calendar
  },
];

const dashboardRoutes = [
];

export default { appRoutes, dashboardRoutes };
